# v0.2.2 Implementation Plan — F15 語音輸入 / F16 自動核准 / F21 新 session

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 dashboard 加三個互不相依的功能：send-prompt 語音輸入（F15）、新 session 資料夾 modal + 開終端（F21）、build/test/install 自動核准開關（F16，含獨立 hook + 最小 installer + 三段式旗標）。

**Architecture:** 沿用 repo 既有 pattern —— server 邏輯抽到 `server/lib/*.js`（可 node:test）、`server/index.js` 只接 `/api/*` 路由；FE 改 `web/ui/{app.js,styles.css}` + `web/dashboard.html`（no-build ES module）；F16 的執行端是 repo 擁有的獨立 PreToolUse hook（`hooks/auto-approve-build.sh`），由 `install-hooks.ps1` 裝進 `~/.claude/`。

**Tech Stack:** Node ≥20 CommonJS、`node:test`、`ws`、vanilla JS ES module、POSIX sh hook（jq）、PowerShell installer。

**前置已驗證（2026-06-09，claude-code-guide）：** Claude Code 多支 PreToolUse hook **依序執行**、`deny > allow > defer`、`allow + 另一支 defer(exit0 無輸出) → 自動核准`、用 `hookSpecificOutput.permissionDecision` schema、separate hook 為官方推薦。→ F16 用獨立第二支 hook，**不需 fallback**。

**Commit 政策：** 本 repo 使用者規則為「不主動 commit / push」。以下各 task 的 **Commit step 為建議切點**；實際 commit／發版由使用者裁示或走 `release` skill。

**實作順序：** Phase A (F15) → Phase B (F21) → Phase C (F16)。三 Phase 互不相依，可平行；同一 `server/index.js` 由 controller 集中編譯/測試一次（fast-mode）。

**集中驗證指令：** `cd server && node --test`（跑所有 `server/test/*.test.js`）。

---

## File Structure（決定 decomposition）

| 檔案 | 責任 | 動作 |
|---|---|---|
| `web/ui/app.js` | 🎤 鈕 + recognition（F15）、F16 控制、F21 modal/button 邏輯 | Modify |
| `web/ui/styles.css` | 🎤 鈕、F16 toggle、F21 modal 樣式 | Modify |
| `web/dashboard.html` | topbar F16 控制 + F21 鈕 markup、F21 modal 容器 | Modify |
| `server/lib/fslist.js` | 列子目錄 / 磁碟根（F21） | **Create** |
| `server/lib/auto-approve-build.js` | F16 三段式旗標狀態機 + hookInstalled 偵測 | **Create** |
| `server/index.js` | 接 `/api/fs/list`、`/api/launch-session`、`/api/auto-approve-build`；啟動對帳 + 關閉清旗標 | Modify |
| `hooks/auto-approve-build.sh` | F16 執行端：單一 build/test/install 指令 + 無害尾管 → allow | **Create** |
| `install-hooks.ps1` | 把 hook 裝進 `~/.claude/hooks/` + 註冊 settings.json | **Create** |
| `server/test/fslist.test.js` | F21 fs/list 單元測試 | **Create** |
| `server/test/auto-approve-build.test.js` | F16 狀態機單元測試 | **Create** |
| `server/test/hook-auto-approve-build.test.js` | F16 hook 行為測試（spawn shell） | **Create** |

---

# Phase A — F15 send-prompt 語音輸入（FE-only、人工測試）

### Task A1: 🎤 語音輸入鈕 + webkitSpeechRecognition

**Files:**
- Modify: `web/ui/app.js`（card render `send-prompt` 區 `:607-610`；新增 recognition 邏輯）
- Modify: `web/ui/styles.css`（`.send-prompt` 區 `:1080`）

- [ ] **Step 1: 在 send-prompt 加 🎤 鈕（render）**

把 `web/ui/app.js:607-610` 的 `.send-prompt` 區塊改成（在 textarea 與送出鈕之間插入 🎤；用 `data-mic` 標記，無 inline handler）：

```js
      <div class="send-prompt">
        <textarea class="send-prompt-input" rows="2" spellcheck="false" placeholder="送 prompt 到此 session…（複製+聚焦+貼上，自行按 Enter 送出）"></textarea>
        <div class="send-prompt-row">
          <button class="action-btn mic-btn" data-mic title="語音輸入（zh-TW，填入後自行檢查送出）" hidden>🎤 語音</button>
          <button class="action-btn send-prompt-btn">✎ 送出 prompt</button>
        </div>
      </div>
```

- [ ] **Step 2: 加 recognition module（app.js，靠近其他 IIFE 區段，例如 `sendPromptToSession` 之後 `:955`）**

```js
  // F15: speech-to-text for the send-prompt textarea (Chrome webkitSpeechRecognition, zh-TW).
  // Fills the textarea; never auto-sends (user reviews + presses 送出 as before).
  const SpeechRec = window.SpeechRecognition || window.webkitSpeechRecognition;
  const micSupported = !!SpeechRec;
  let micRec = null;        // active recognition instance
  let micBtnActive = null;  // the button currently recording

  function startMic(btn, textarea) {
    if (!micSupported || micRec) return;
    const rec = new SpeechRec();
    rec.lang = 'zh-TW';
    rec.interimResults = true;
    rec.continuous = false;
    const base = textarea.value;            // append onto existing text
    let finalAdd = '';
    rec.onresult = (ev) => {
      let interim = '';
      for (let i = ev.resultIndex; i < ev.results.length; i++) {
        const tr = ev.results[i][0].transcript;
        if (ev.results[i].isFinal) finalAdd += tr; else interim += tr;
      }
      textarea.value = base + finalAdd + interim;
    };
    rec.onerror = (ev) => { pushToast({ title: '語音輸入失敗', msg: ev.error || '辨識錯誤（檢查麥克風權限）' }); };
    rec.onend = () => { micRec = null; if (micBtnActive) { micBtnActive.classList.remove('recording'); micBtnActive.textContent = '🎤 語音'; micBtnActive = null; } };
    micRec = rec; micBtnActive = btn;
    btn.classList.add('recording'); btn.textContent = '⏹ 停止';
    try { rec.start(); } catch (e) { rec.onend(); }
  }
  function stopMic() { if (micRec) { try { micRec.stop(); } catch {} } }
```

- [ ] **Step 3: 接到既有 card click dispatch（`sessionsContainer.addEventListener('click', …)` `:733`）**

在該 click handler 內，`sendBtn` 判斷附近，加入 mic 分支：

```js
    const micBtn = e.target.closest('.mic-btn');
    if (micBtn) {
      const card = micBtn.closest('.card');
      const ta = card?.querySelector('.send-prompt-input');
      if (!ta) return;
      if (micBtn === micBtnActive) stopMic(); else { stopMic(); startMic(micBtn, ta); }
      return;
    }
```

- [ ] **Step 4: render 後顯示 🎤（只在支援時）**

card 是動態 render 的，鈕預設 `hidden`。在 render session 卡片後的收尾處（例如既有 render 迴圈結束、或 `sessionsContainer` 更新後），統一解除隱藏：

```js
  // after cards are (re)rendered — reveal mic buttons only where supported
  function revealMicButtons() {
    if (!micSupported) return;
    $$('.mic-btn[hidden]').forEach((b) => { b.hidden = false; });
  }
```

並在既有「render 完一輪卡片」的地方呼叫 `revealMicButtons()`（與 `startRuntimeTicker()` 同層級的 render 收尾）。

- [ ] **Step 5: CSS（`web/ui/styles.css`，`.send-prompt` 附近 `:1080`）**

```css
  .send-prompt-row { display: flex; gap: 6px; }
  .send-prompt-row .send-prompt-btn { flex: 1; }
  .mic-btn { flex: 0 0 auto; }
  .mic-btn.recording { background: var(--accent); color: #fff; border-color: var(--accent); }
```

- [ ] **Step 6: 人工測試清單（瀏覽器 API 無法 node 自動測 — 明列）**

Run: `cd server && node index.js` → Chrome 開 `http://127.0.0.1:7878/`
- [ ] 展開一張卡 → 看到「🎤 語音」鈕（Chrome）。
- [ ] 點 🎤 → 變「⏹ 停止」+ 變色 → 講中文 → 文字即時填入 textarea（保留原有文字後 append）。
- [ ] 再點 ⏹ 或講完 → 鈕復位；textarea 內容保留，**未自動送出**。
- [ ] 點「✎ 送出 prompt」走原流程。
- [ ] 拒絕麥克風權限 → toast「語音輸入失敗」+ 鈕復位。
- [ ] Firefox/Edge-無 webkitSpeech → 🎤 不出現（`micSupported=false`）。

- [ ] **Step 7: Commit（建議切點）**

```bash
git add web/ui/app.js web/ui/styles.css
git commit -m "feat(F15): send-prompt 語音輸入（webkitSpeechRecognition zh-TW）"
```

---

# Phase B — F21 新 session：資料夾 modal → 開終端跑 claude

### Task B1: `server/lib/fslist.js` + 單元測試（TDD）

**Files:**
- Create: `server/lib/fslist.js`
- Test: `server/test/fslist.test.js`

- [ ] **Step 1: 寫失敗測試 `server/test/fslist.test.js`**

```js
const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const os = require('os');
const path = require('path');
const { listDir } = require('../lib/fslist');

test('listDir 列出子目錄（排序、隱藏 dot/$、排除檔案）', () => {
  const root = fs.mkdtempSync(path.join(os.tmpdir(), 'fslist-'));
  fs.mkdirSync(path.join(root, 'beta'));
  fs.mkdirSync(path.join(root, 'alpha'));
  fs.mkdirSync(path.join(root, '.hidden'));
  fs.writeFileSync(path.join(root, 'a.txt'), 'x');
  const r = listDir(root);
  assert.deepStrictEqual(r.dirs, ['alpha', 'beta']);
  assert.strictEqual(r.path, path.resolve(root));
  assert.strictEqual(r.parent, path.dirname(path.resolve(root)));
});

test('listDir 空 path → 回磁碟機根 + parent=null', () => {
  const r = listDir('');
  assert.ok(Array.isArray(r.drives));
  assert.strictEqual(r.parent, null);
});

test('listDir 不存在路徑 → throw', () => {
  assert.throws(() => listDir(path.join(os.tmpdir(), 'no-such-dir-xyz123')));
});
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `cd server && node --test test/fslist.test.js`
Expected: FAIL（`Cannot find module '../lib/fslist'`）

- [ ] **Step 3: 實作 `server/lib/fslist.js`**

```js
const fs = require('fs');
const path = require('path');

// List immediate subdirectories of `p`. Empty `p` → Windows drive roots.
// Returns { path, parent, drives, dirs }. Throws on missing / non-directory path.
function listDir(p) {
  if (!p) return { path: '', parent: null, drives: driveRoots(), dirs: [] };
  const abs = path.resolve(p);
  const st = fs.statSync(abs);                 // throws if missing
  if (!st.isDirectory()) throw new Error('not a directory');
  const dirs = fs.readdirSync(abs, { withFileTypes: true })
    .filter((e) => { try { return e.isDirectory(); } catch { return false; } })
    .map((e) => e.name)
    .filter((n) => !n.startsWith('.') && !n.startsWith('$'))   // hide dotfiles / $Recycle.Bin
    .sort((a, b) => a.localeCompare(b));
  const parent = path.dirname(abs);
  return { path: abs, parent: parent === abs ? null : parent, drives: [], dirs };
}

function driveRoots() {
  const out = [];
  for (let c = 67; c <= 90; c++) {              // C: .. Z:
    const d = String.fromCharCode(c) + ':\\';
    try { if (fs.existsSync(d)) out.push(d); } catch {}
  }
  return out;
}

module.exports = { listDir, driveRoots };
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `cd server && node --test test/fslist.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit（建議）**

```bash
git add server/lib/fslist.js server/test/fslist.test.js
git commit -m "feat(F21): fslist 列子目錄 lib + 測試"
```

### Task B2: server `/api/fs/list` + `/api/launch-session`

**Files:**
- Modify: `server/index.js`（require 區 `:1-12`；路由區 `:284-373`；新增 spawn helper）

- [ ] **Step 1: require 新 lib（`server/index.js` 頂部 require 區，`:12` 之後）**

```js
const { listDir } = require('./lib/fslist');
```

- [ ] **Step 2: 在路由區（`/api/refresh` 等同層，`server/index.js:301` 之後）加 `/api/fs/list`**

```js
  if (req.method === 'GET' && url.pathname === '/api/fs/list') {
    try {
      const result = listDir(url.searchParams.get('path') || '');
      res.writeHead(200, { 'Content-Type': 'application/json; charset=utf-8' });
      res.end(JSON.stringify(result));
    } catch (e) {
      res.writeHead(400, { 'Content-Type': 'application/json; charset=utf-8' });
      res.end(JSON.stringify({ error: e.message }));
    }
    return;
  }
```

- [ ] **Step 3: 加 spawn helper（`server/index.js`，靠近 F19 restart spawn `:324`，或檔案下方 helper 區）**

```js
  // F21: open a new terminal in <dir> running `claude`. Prefer Windows Terminal (wt -d);
  // fall back to a detached PowerShell window. Same detached/unref pattern as /api/restart.
  function launchClaudeSession(dir) {
    const { spawn } = require('child_process');
    const psDir = dir.replace(/'/g, "''");
    const tryPs = () => spawn('cmd', ['/c', 'start', 'powershell', '-NoExit', '-Command',
      `Set-Location -LiteralPath '${psDir}'; claude`], { detached: true, stdio: 'ignore' });
    let child;
    try {
      child = spawn('wt', ['-d', dir, 'cmd', '/k', 'claude'], { detached: true, stdio: 'ignore' });
      child.on('error', () => { try { tryPs().unref(); } catch {} });   // wt not installed → fallback
    } catch {
      child = tryPs();
    }
    try { child.unref(); } catch {}
  }
```

- [ ] **Step 4: 加 `/api/launch-session`（路由區）**

```js
  if (req.method === 'POST' && url.pathname === '/api/launch-session') {
    let body = '';
    req.on('data', (c) => { body += c.toString(); });
    req.on('end', () => {
      let dir = '';
      try { dir = String(JSON.parse(body || '{}').dir || ''); } catch {}
      try {
        if (!dir || !fs.statSync(dir).isDirectory()) throw new Error('not a directory');
      } catch (e) {
        res.writeHead(400, { 'Content-Type': 'application/json; charset=utf-8' });
        res.end(JSON.stringify({ error: 'invalid dir: ' + (e.message || dir) }));
        return;
      }
      try {
        launchClaudeSession(dir);
        res.writeHead(200, { 'Content-Type': 'application/json; charset=utf-8' });
        res.end(JSON.stringify({ ok: true, dir }));
      } catch (e) {
        res.writeHead(500, { 'Content-Type': 'application/json; charset=utf-8' });
        res.end(JSON.stringify({ error: e.message }));
      }
    });
    return;
  }
```

- [ ] **Step 5: 驗證（人工 + 集中編譯）**

Run: `cd server && node -e "require('./index.js')"` 起服務不報錯（Ctrl+C 結束），或直接 `node index.js`。
- [ ] `curl "http://127.0.0.1:7878/api/fs/list"` → 回 `{drives:[...], dirs:[], parent:null}`。
- [ ] `curl "http://127.0.0.1:7878/api/fs/list?path=D:\\projects"` → 回該層子目錄。
- [ ] `curl -X POST http://127.0.0.1:7878/api/launch-session -d '{"dir":"D:\\nope"}'` → 400。

- [ ] **Step 6: Commit（建議）**

```bash
git add server/index.js
git commit -m "feat(F21): /api/fs/list + /api/launch-session（wt -d，fallback powershell）"
```

### Task B3: FE 新 session modal（topbar 鈕 + 目錄瀏覽 + 記憶上次 + 確認）

**Files:**
- Modify: `web/dashboard.html`（topbar-actions `:31-62` 加鈕；body 末加 modal 容器）
- Modify: `web/ui/app.js`（modal 邏輯）
- Modify: `web/ui/styles.css`（modal 樣式）

- [ ] **Step 1: topbar 加「＋ 新 session」鈕（`web/dashboard.html`，`:55` 的 open-claude-btn 之後）**

```html
      <button class="icon-btn" id="new-session-btn" title="新 session：選資料夾 → 開終端跑 claude">
        <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M8 3v10M3 8h10"/></svg>
      </button>
```

- [ ] **Step 2: body 末加 modal 容器（`web/dashboard.html`，`</body>` 前）**

```html
  <div class="fs-modal hidden" id="fs-modal">
    <div class="fs-modal-card">
      <div class="fs-modal-head">
        <span class="fs-modal-title">選擇資料夾 → 開新 Claude session</span>
        <button class="icon-btn" id="fs-close">✕</button>
      </div>
      <div class="fs-crumb" id="fs-crumb"></div>
      <div class="fs-list" id="fs-list"></div>
      <div class="fs-modal-foot">
        <span class="fs-current" id="fs-current">—</span>
        <button class="action-btn primary" id="fs-open" disabled>在此開啟</button>
      </div>
    </div>
  </div>
```

- [ ] **Step 3: modal 邏輯（`web/ui/app.js`，topbar handler 區，例如 restart-btn handler `:1141` 附近）**

```js
  // F21: new-session folder picker modal.
  const fsModal = $('#fs-modal'), fsList = $('#fs-list'), fsCrumb = $('#fs-crumb'),
        fsCurrent = $('#fs-current'), fsOpen = $('#fs-open');
  let fsCurPath = '';

  async function fsLoad(p) {
    try {
      const res = await fetch('/api/fs/list?path=' + encodeURIComponent(p || ''));
      const data = await res.json();
      if (!res.ok) { pushToast({ title: '讀目錄失敗', msg: data.error || '' }); return; }
      fsCurPath = data.path || '';
      fsCurrent.textContent = fsCurPath || '（磁碟機）';
      fsOpen.disabled = !fsCurPath;            // 磁碟機根清單層不可直接開
      if (fsCurPath) localStorage.setItem('fs.lastPath', fsCurPath);
      // breadcrumb: 上層 + 目前
      fsCrumb.innerHTML = '';
      if (data.parent !== null || data.path) {
        const up = document.createElement('button');
        up.className = 'fs-up'; up.textContent = '⬆ 上層';
        up.onclick = () => fsLoad(data.parent === null ? '' : data.parent);
        fsCrumb.appendChild(up);
      }
      // list: drives (root) or dirs
      fsList.innerHTML = '';
      const items = (data.drives && data.drives.length) ? data.drives
        : data.dirs.map((d) => (fsCurPath.endsWith('\\') ? fsCurPath + d : fsCurPath + '\\' + d));
      for (const full of items) {
        const row = document.createElement('button');
        row.className = 'fs-row'; row.textContent = '📁 ' + full.replace(/\\$/, '').split('\\').pop() || full;
        row.title = full;
        row.onclick = () => fsLoad(full);
        fsList.appendChild(row);
      }
      if (!items.length) fsList.innerHTML = '<div class="fs-empty">（無子資料夾）</div>';
    } catch (err) { pushToast({ title: '讀目錄失敗', msg: err.message }); }
  }

  $('#new-session-btn').addEventListener('click', () => {
    fsModal.classList.remove('hidden');
    fsLoad(localStorage.getItem('fs.lastPath') || '');
  });
  $('#fs-close').addEventListener('click', () => fsModal.classList.add('hidden'));
  fsModal.addEventListener('click', (e) => { if (e.target === fsModal) fsModal.classList.add('hidden'); });
  fsOpen.addEventListener('click', async () => {
    if (!fsCurPath) return;
    if (!confirm(`將在這個資料夾開新 Claude session：\n${fsCurPath}`)) return;
    try {
      const res = await fetch('/api/launch-session', {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ dir: fsCurPath }),
      });
      const data = await res.json().catch(() => ({}));
      if (res.ok && data.ok) { pushToast({ title: '已開新 session', msg: fsCurPath }); fsModal.classList.add('hidden'); }
      else pushToast({ title: '開新 session 失敗', msg: data.error || `HTTP ${res.status}` });
    } catch (err) { pushToast({ title: '開新 session 失敗', msg: err.message }); }
  });
```

- [ ] **Step 4: CSS（`web/ui/styles.css` 末）**

```css
  .fs-modal { position: fixed; inset: 0; background: rgba(0,0,0,.35); display: flex; align-items: center; justify-content: center; z-index: 50; }
  .fs-modal.hidden { display: none; }
  .fs-modal-card { width: min(560px, 92vw); max-height: 80vh; display: flex; flex-direction: column; background: var(--surface); border: 1px solid var(--border); border-radius: 12px; box-shadow: 0 12px 40px rgba(0,0,0,.25); overflow: hidden; }
  .fs-modal-head { display: flex; align-items: center; justify-content: space-between; padding: 12px 14px; border-bottom: 1px solid var(--border); }
  .fs-modal-title { font-weight: 600; }
  .fs-crumb { padding: 8px 14px; }
  .fs-up { background: var(--surface-2); border: 1px solid var(--border); border-radius: 6px; padding: 4px 10px; cursor: pointer; }
  .fs-list { flex: 1; overflow-y: auto; padding: 4px 8px; }
  .fs-row { display: block; width: 100%; text-align: left; background: none; border: none; padding: 7px 8px; border-radius: 6px; cursor: pointer; }
  .fs-row:hover { background: var(--surface-2); }
  .fs-empty { padding: 12px; color: var(--text-faint); }
  .fs-modal-foot { display: flex; align-items: center; gap: 10px; padding: 12px 14px; border-top: 1px solid var(--border); }
  .fs-current { flex: 1; font-size: 12.5px; color: var(--text-muted); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
```

- [ ] **Step 5: 人工測試清單（spawn 終端無法 node 自動測 — 明列）**

Run: `cd server && node index.js` → 瀏覽器開 dashboard
- [ ] topbar 看到「＋」鈕 → 點 → modal 跳出，顯示磁碟機清單。
- [ ] 點磁碟機 → 進入；逐層點資料夾深入；「⬆ 上層」回上層。
- [ ] 底部「在此開啟」顯示目前路徑 → 點 → confirm → 真的開新終端視窗跑起 `claude`。
- [ ] 關 modal 再開 → 定位回上次路徑（localStorage `fs.lastPath`）。
- [ ] 有 Windows Terminal 走 `wt`；沒有 → fallback PowerShell 視窗（兩種環境各驗一次）。

- [ ] **Step 6: Commit（建議）**

```bash
git add web/dashboard.html web/ui/app.js web/ui/styles.css
git commit -m "feat(F21): 新 session 資料夾 modal（逐層瀏覽 + 記憶上次 + 確認）"
```

---

# Phase C — F16 自動核准 build/test/install（hook + installer + 三段式旗標 + FE）

### Task C1: `hooks/auto-approve-build.sh` + hook 行為測試（TDD）

**Files:**
- Create: `hooks/auto-approve-build.sh`
- Test: `server/test/hook-auto-approve-build.test.js`

- [ ] **Step 1: 寫失敗測試 `server/test/hook-auto-approve-build.test.js`（spawn shell，餵 JSON stdin，斷言 stdout）**

```js
const { test } = require('node:test');
const assert = require('node:assert');
const cp = require('child_process');
const fs = require('fs');
const os = require('os');
const path = require('path');

const HOOK = path.resolve(__dirname, '..', '..', 'hooks', 'auto-approve-build.sh');

// run the hook with a temp HOME; `enabled` controls the flag file presence.
function runHook(cmd, { enabled = true } = {}) {
  const home = fs.mkdtempSync(path.join(os.tmpdir(), 'aabhook-'));
  fs.mkdirSync(path.join(home, '.claude'), { recursive: true });
  if (enabled) fs.writeFileSync(path.join(home, '.claude', 'auto-approve-build.enabled'), '');
  const input = JSON.stringify({ tool_name: 'Bash', tool_input: { command: cmd }, session_id: 't' });
  const out = cp.execFileSync('bash', [HOOK], { input, env: { ...process.env, HOME: home }, encoding: 'utf8' });
  return out.trim();
}
const isAllow = (s) => s.includes('"permissionDecision":"allow"');

test('flag off → defer (no output) even for whitelisted cmd', () => {
  assert.strictEqual(runHook('mvn test', { enabled: false }), '');
});
test('single mvn test → allow', () => { assert.ok(isAllow(runHook('mvn test'))); });
test('npm install → allow (install in scope)', () => { assert.ok(isAllow(runHook('npm install'))); });
test('mvn test | tail -5 → allow (harmless tail)', () => { assert.ok(isAllow(runHook('mvn test | tail -5'))); });
test('mvn test ; echo done → allow', () => { assert.ok(isAllow(runHook('mvn test ; echo done'))); });
test('redirect mvn test > out → deny(defer)', () => { assert.strictEqual(runHook('mvn test > out'), ''); });
test('chaining npm install && rm -rf x → deny(defer)', () => { assert.strictEqual(runHook('npm install && rm -rf x'), ''); });
test('subshell $(curl evil) → deny(defer)', () => { assert.strictEqual(runHook('mvn test $(curl evil)'), ''); });
test('non-whitelisted cmd ls -la → deny(defer)', () => { assert.strictEqual(runHook('ls -la'), ''); });
test('pipe to non-readonly mvn test | rm → deny(defer)', () => { assert.strictEqual(runHook('mvn test | rm x'), ''); });
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `cd server && node --test test/hook-auto-approve-build.test.js`
Expected: FAIL（hook 檔不存在 → execFileSync throw）

- [ ] **Step 3: 實作 `hooks/auto-approve-build.sh`**

```sh
#!/bin/sh
# PreToolUse hook (F16) — auto-approve a SINGLE clean build/test/lint/install command,
# optionally followed by a HARMLESS read-only tail (| tail/head/grep ; echo ; ls).
# Independent second hook; does NOT touch auto-approve.sh. Layered:
#   (0) flag gate     — nothing unless ~/.claude/auto-approve-build.enabled exists
#   (1) hard deny     — redirect/subshell/chaining/destructive verbs → defer (never allow)
#   (2) allow-list    — segment 1 = whitelisted main verb; later segments = harmless only
#   (3) audit log     — every allow appended to ~/.claude/auto-approve.log
# Print nothing + exit 0 = defer (fall through to normal prompt). deny>allow>defer compose.

input=$(cat)
[ -f "$HOME/.claude/auto-approve-build.enabled" ] || exit 0

tool=$(printf '%s' "$input" | jq -r '.tool_name // empty' 2>/dev/null)
[ "$tool" = "Bash" ] || exit 0
cmd=$(printf '%s' "$input" | jq -r '.tool_input.command // empty' 2>/dev/null)
[ -z "$cmd" ] && exit 0

# (1) hard deny veto: redirects, subshell, chaining, background (& and &&), destructive verbs.
# NOTE: a lone `&` MUST be denied — `tr` below only splits on | and ; , so `cmd & evil`
# would otherwise stay one segment and pass the is_main start-anchored check.
deny='>>|[>`&]|\$\(|\|\||(^|[^[:alnum:]_])(rm|rmdir|mv|cp|sudo|chmod|chown|kill|taskkill|dd|mkfs|del|format|truncate|tee|ln)([^[:alnum:]_]|$)'
if printf '%s' "$cmd" | grep -Eq "$deny"; then exit 0; fi

# main verb whitelist (segment 1 must match one of these patterns from its start)
is_main() {
  printf '%s' "$1" | grep -Eq '^[[:space:]]*(mvn([[:space:]]+-[^[:space:]]+)*[[:space:]]+(compile|test-compile|verify|test)([[:space:]]|$)|(gradle|\./gradlew)[[:space:]]+(build|assemble|test)|tsc([[:space:]]+--noEmit)?([[:space:]]|$)|(npm|pnpm|yarn)[[:space:]]+(run[[:space:]]+build|test|install|ci)([[:space:]]|$)|yarn([[:space:]]|$)|vite[[:space:]]+build|go[[:space:]]+(build|vet|test|get)([[:space:]]|$)|go[[:space:]]+mod[[:space:]]+(download|tidy)|cargo[[:space:]]+(build|check|test|fetch)([[:space:]]|$)|make([[:space:]]|$)|dotnet[[:space:]]+(build|test|restore)|(jest|vitest|mocha|playwright|cypress|pytest|mypy)([[:space:]]|$)|(python|python3)[[:space:]]+-m[[:space:]]+pytest|eslint([[:space:]]|$)|prettier[[:space:]]+--check|ruff[[:space:]]+check|gofmt[[:space:]]+-l|(pip|pip3)[[:space:]]+install|poetry[[:space:]]+install|pipenv[[:space:]]+install|uv[[:space:]]+pip[[:space:]]+install|bundle[[:space:]]+install|composer[[:space:]]+install)'
}

# split on | and ; (the only combinators left after hard-deny); validate each segment.
segs=$(printf '%s' "$cmd" | tr '|;' '\n\n')
i=0; ok=1
while IFS= read -r seg; do
  seg=$(printf '%s' "$seg" | sed 's/^[[:space:]]*//; s/[[:space:]]*$//')
  [ -z "$seg" ] && continue
  i=$((i+1))
  if [ "$i" -eq 1 ]; then
    is_main "$seg" || { ok=0; break; }
  else
    first=$(printf '%s' "$seg" | awk '{print $1}')
    case "$first" in
      tail|head|grep|echo|ls|cat|wc) : ;;
      *) ok=0; break ;;
    esac
  fi
done <<EOF
$segs
EOF

[ "$ok" -eq 1 ] || exit 0

ts=$(date '+%Y-%m-%d %H:%M:%S' 2>/dev/null)
sid=$(printf '%s' "$input" | jq -r '.session_id // ""' 2>/dev/null)
printf '%s\tsid=%s\tBash(build)\t%s\n' "$ts" "$sid" "$cmd" >> "$HOME/.claude/auto-approve.log" 2>/dev/null
printf '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow","permissionDecisionReason":"auto-build: single cmd + harmless tail"}}\n'
exit 0
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `cd server && node --test test/hook-auto-approve-build.test.js`
Expected: PASS（10 tests）。若 `bash`/`jq` 不在 PATH → 安裝/補 PATH（既有 auto-approve.sh 已依賴 jq，環境應已具備）。

- [ ] **Step 5: Commit（建議）**

```bash
git add hooks/auto-approve-build.sh server/test/hook-auto-approve-build.test.js
git commit -m "feat(F16): auto-approve-build hook（單一 build/test/install + 無害尾管）+ 測試"
```

### Task C2: `install-hooks.ps1`（最小 installer）

**Files:**
- Create: `install-hooks.ps1`

- [ ] **Step 1: 寫 installer**

```powershell
# install-hooks.ps1 — install the F16 auto-approve-build PreToolUse hook (idempotent).
# Copies hooks/auto-approve-build.sh → ~/.claude/hooks/ and registers it in settings.json.
$ErrorActionPreference = 'Stop'
$claude   = Join-Path $HOME '.claude'
$hooksDir = Join-Path $claude 'hooks'
$settings = Join-Path $claude 'settings.json'
$src      = Join-Path $PSScriptRoot 'hooks\auto-approve-build.sh'
$dst      = Join-Path $hooksDir 'auto-approve-build.sh'

New-Item -ItemType Directory -Force -Path $hooksDir | Out-Null
Copy-Item -Force $src $dst
Write-Host "copied hook -> $dst"

if (Test-Path $settings) { $cfg = Get-Content -Raw $settings | ConvertFrom-Json }
else { $cfg = [pscustomobject]@{} }

if (-not $cfg.PSObject.Properties['hooks'])       { $cfg | Add-Member hooks ([pscustomobject]@{}) }
if (-not $cfg.hooks.PSObject.Properties['PreToolUse']) { $cfg.hooks | Add-Member PreToolUse @() }

$cmd = 'bash ~/.claude/hooks/auto-approve-build.sh'
$already = $false
foreach ($grp in @($cfg.hooks.PreToolUse)) {
  foreach ($h in @($grp.hooks)) { if ($h.command -eq $cmd) { $already = $true } }
}

if ($already) {
  Write-Host 'hook already registered in settings.json (skip)'
} else {
  $entry = [pscustomobject]@{ matcher = 'Bash'; hooks = @([pscustomobject]@{ type = 'command'; command = $cmd }) }
  # force array even if PreToolUse had a single element
  $cfg.hooks.PreToolUse = @($cfg.hooks.PreToolUse) + $entry
  ($cfg | ConvertTo-Json -Depth 20) | Set-Content -Encoding UTF8 $settings
  Write-Host 'registered hook in settings.json'
}
Write-Host 'done. Restart Claude Code sessions for the hook to take effect.'
```

- [ ] **Step 2: 驗證（人工 — 改 user-global 設定，謹慎）**

先備份：`cp ~/.claude/settings.json ~/.claude/settings.json.bak`
Run: `powershell -ExecutionPolicy Bypass -File install-hooks.ps1`
- [ ] `~/.claude/hooks/auto-approve-build.sh` 存在。
- [ ] `~/.claude/settings.json` 的 `hooks.PreToolUse` 多一筆 `matcher:"Bash"` + 該 command；**原有 auto-approve.sh 那筆完好**。
- [ ] 再跑一次 installer → 印「already registered (skip)」、settings.json 不再變動（idempotent）。
- [ ] ⚠️ settings.json 仍是合法 JSON（`jq . ~/.claude/settings.json` 不報錯）。

- [ ] **Step 3: Commit（建議）**

```bash
git add install-hooks.ps1
git commit -m "feat(F16): install-hooks.ps1（裝 auto-approve-build hook + 註冊 settings.json）"
```

### Task C3: `server/lib/auto-approve-build.js` 狀態機 + server 接線（TDD）

**Files:**
- Create: `server/lib/auto-approve-build.js`
- Test: `server/test/auto-approve-build.test.js`
- Modify: `server/index.js`（require、endpoints、啟動對帳、關閉清旗標）

- [ ] **Step 1: 寫失敗測試 `server/test/auto-approve-build.test.js`**

```js
const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const os = require('os');
const path = require('path');
const m = require('../lib/auto-approve-build');

function tmpHome() {
  const h = fs.mkdtempSync(path.join(os.tmpdir(), 'aab-'));
  fs.mkdirSync(path.join(h, '.claude'), { recursive: true });
  return h;
}

test('default state = off', () => { assert.strictEqual(m.readState(tmpHome()), 'off'); });

test('session = enabled only', () => {
  const h = tmpHome();
  assert.strictEqual(m.setState(h, 'session'), 'session');
  assert.ok(fs.existsSync(m.enabledPath(h)));
  assert.ok(!fs.existsSync(m.persistPath(h)));
});

test('permanent = enabled + persist', () => {
  const h = tmpHome();
  m.setState(h, 'permanent');
  assert.strictEqual(m.readState(h), 'permanent');
  assert.ok(fs.existsSync(m.persistPath(h)));
});

test('off clears both', () => {
  const h = tmpHome(); m.setState(h, 'permanent'); m.setState(h, 'off');
  assert.strictEqual(m.readState(h), 'off');
});

test('startup reconcile: persist re-applies enabled', () => {
  const h = tmpHome(); m.setState(h, 'permanent');
  fs.rmSync(m.enabledPath(h), { force: true });
  assert.strictEqual(m.reconcileOnStartup(h), 'permanent');
  assert.ok(fs.existsSync(m.enabledPath(h)));
});

test('startup reconcile: no persist clears stray enabled (crash-safe → off)', () => {
  const h = tmpHome(); m.setState(h, 'session');
  assert.strictEqual(m.reconcileOnStartup(h), 'off');
});

test('shutdown clears session but keeps permanent', () => {
  const a = tmpHome(); m.setState(a, 'session'); m.clearOnShutdown(a);
  assert.strictEqual(m.readState(a), 'off');
  const b = tmpHome(); m.setState(b, 'permanent'); m.clearOnShutdown(b);
  assert.strictEqual(m.readState(b), 'permanent');
});

test('hookInstalled reflects hook file presence', () => {
  const h = tmpHome();
  assert.strictEqual(m.hookInstalled(h), false);
  fs.mkdirSync(path.join(h, '.claude', 'hooks'), { recursive: true });
  fs.writeFileSync(path.join(h, '.claude', 'hooks', 'auto-approve-build.sh'), '');
  assert.strictEqual(m.hookInstalled(h), true);
});
```

- [ ] **Step 2: 跑測試確認 fail**

Run: `cd server && node --test test/auto-approve-build.test.js`
Expected: FAIL（`Cannot find module '../lib/auto-approve-build'`）

- [ ] **Step 3: 實作 `server/lib/auto-approve-build.js`**

```js
const fs = require('fs');
const path = require('path');
const os = require('os');

// F16 flag files under ~/.claude:
//   auto-approve-build.enabled  → the hook reads this (presence = active)
//   auto-approve-build.persist  → "permanent" marker (survives dashboard restart)
// States: off (neither) / session (enabled only) / permanent (enabled + persist).
function claudeDir(home)     { return path.join(home || os.homedir(), '.claude'); }
function enabledPath(home)   { return path.join(claudeDir(home), 'auto-approve-build.enabled'); }
function persistPath(home)   { return path.join(claudeDir(home), 'auto-approve-build.persist'); }
function installedHook(home) { return path.join(claudeDir(home), 'hooks', 'auto-approve-build.sh'); }

function readState(home) {
  const en = fs.existsSync(enabledPath(home));
  const pe = fs.existsSync(persistPath(home));
  if (en && pe) return 'permanent';
  if (en) return 'session';
  return 'off';
}

function setState(home, state) {
  fs.mkdirSync(claudeDir(home), { recursive: true });
  if (state === 'off')            { rm(enabledPath(home)); rm(persistPath(home)); }
  else if (state === 'session')   { touch(enabledPath(home)); rm(persistPath(home)); }
  else if (state === 'permanent') { touch(enabledPath(home)); touch(persistPath(home)); }
  else throw new Error('invalid state: ' + state);
  return readState(home);
}

// dashboard startup: persist → ensure enabled; else clear stray enabled (default off, crash-safe)
function reconcileOnStartup(home) {
  if (fs.existsSync(persistPath(home))) touch(enabledPath(home));
  else rm(enabledPath(home));
  return readState(home);
}

// dashboard shutdown: drop session-scoped enabled (permanent keeps it via persist)
function clearOnShutdown(home) {
  if (!fs.existsSync(persistPath(home))) rm(enabledPath(home));
}

function hookInstalled(home) { return fs.existsSync(installedHook(home)); }

function touch(p) { try { fs.writeFileSync(p, ''); } catch {} }
function rm(p)    { try { fs.rmSync(p, { force: true }); } catch {} }

module.exports = { readState, setState, reconcileOnStartup, clearOnShutdown, hookInstalled, enabledPath, persistPath };
```

- [ ] **Step 4: 跑測試確認 pass**

Run: `cd server && node --test test/auto-approve-build.test.js`
Expected: PASS（8 tests）

- [ ] **Step 5: server 接線 — require（`server/index.js` 頂部，`:12` 之後）**

```js
const aab = require('./lib/auto-approve-build');
```

- [ ] **Step 6: 加 endpoints（路由區，`/api/fs/list` 同層）**

```js
  if (req.method === 'GET' && url.pathname === '/api/auto-approve-build') {
    res.writeHead(200, { 'Content-Type': 'application/json; charset=utf-8' });
    res.end(JSON.stringify({ hookInstalled: aab.hookInstalled(), state: aab.readState() }));
    return;
  }
  if (req.method === 'POST' && url.pathname === '/api/auto-approve-build') {
    let body = '';
    req.on('data', (c) => { body += c.toString(); });
    req.on('end', () => {
      let state = 'off';
      try { state = String(JSON.parse(body || '{}').state || 'off'); } catch {}
      try {
        const now = aab.setState(undefined, state);
        res.writeHead(200, { 'Content-Type': 'application/json; charset=utf-8' });
        res.end(JSON.stringify({ ok: true, state: now }));
      } catch (e) {
        res.writeHead(400, { 'Content-Type': 'application/json; charset=utf-8' });
        res.end(JSON.stringify({ error: e.message }));
      }
    });
    return;
  }
```

- [ ] **Step 7: 啟動對帳（`server.on('listening')`，`server/index.js:444-447`，registry.start() 旁）**

```js
server.on('listening', () => {
  console.log(`[claude-mgmt] dashboard server listening on http://${HOST}:${PORT}`);
  try { aab.reconcileOnStartup(); } catch {}   // F16: default each launch = off (unless persist)
  registry.start();
});
```

- [ ] **Step 8: 關閉清旗標（3 處：`/api/shutdown` `:313`、`/api/restart` relaunch 前 `:331`、SIGINT `:463`）**

`/api/shutdown`（`setTimeout(() => process.exit(0), 150)` 之前）：
```js
    try { aab.clearOnShutdown(); } catch {}
```
`/api/restart`（`relaunch` 內、`process.exit(0)` 之前；或 `try { server.close(); }` 之前）：
```js
    try { aab.clearOnShutdown(); } catch {}
```
`SIGINT` handler（`registry.stop();` 之後）：
```js
  try { aab.clearOnShutdown(); } catch {}
```

- [ ] **Step 9: 集中驗證**

Run: `cd server && node --test`（全部 test 應綠）
Run: `node index.js` → 另開 shell：
- [ ] `curl http://127.0.0.1:7878/api/auto-approve-build` → `{hookInstalled:true,state:"off"}`（已跑過 installer）。
- [ ] `curl -X POST .../api/auto-approve-build -d '{"state":"session"}'` → `{ok:true,state:"session"}`；`~/.claude/auto-approve-build.enabled` 出現。
- [ ] Ctrl+C 結束 server → `.enabled` 被清（session 態）。
- [ ] 設 `permanent` → 重啟 server → `state` 仍 `permanent`、`.enabled` 在。

- [ ] **Step 10: Commit（建議）**

```bash
git add server/lib/auto-approve-build.js server/test/auto-approve-build.test.js server/index.js
git commit -m "feat(F16): 三段式旗標狀態機 + /api/auto-approve-build + 啟動對帳/關閉清旗標"
```

### Task C4: FE — F16 控制（主開關 + 永久 checkbox + capability gate）

**Files:**
- Modify: `web/dashboard.html`（settings-panel 內加一段）
- Modify: `web/ui/app.js`（讀/寫狀態、gate）
- Modify: `web/ui/styles.css`（沿用既有 `.switch` 樣式，必要時微調）

- [ ] **Step 1: settings-panel 加 F16 區（`web/dashboard.html`，`:50` notify-toggle 那 row 之後，`</div>` 收 settings-panel 前）**

```html
          <div class="settings-row">
            <span class="settings-label">自動核准 build/test/install<span class="settings-hint" id="aab-hint">需先跑 install-hooks.ps1</span></span>
            <button class="switch" id="aab-toggle" role="switch" aria-checked="false" title="開＝本次 dashboard 開著時自動核准乾淨的 build/test/install 指令"><span class="switch-knob"></span></button>
          </div>
          <div class="settings-row">
            <span class="settings-label" style="padding-left:10px;">└ 永久套用<span class="settings-hint">跨重啟保留（否則關 dashboard 即失效）</span></span>
            <input type="checkbox" id="aab-persist" />
          </div>
```

- [ ] **Step 2: app.js 邏輯（settings 區附近，notifyToggle handler `:216` 後）**

```js
  // F16: auto-approve build/test/install control (3-state: off / session / permanent).
  const aabToggle = $('#aab-toggle'), aabPersist = $('#aab-persist'), aabHint = $('#aab-hint');

  function renderAab({ hookInstalled, state }) {
    const disabled = !hookInstalled;
    aabToggle.disabled = disabled; aabPersist.disabled = disabled;
    aabToggle.style.opacity = disabled ? '.4' : '';
    aabHint.textContent = disabled ? '需先跑 install-hooks.ps1（hook 未安裝）'
                                   : (state === 'permanent' ? '永久套用中' : state === 'session' ? '本次有效' : '關（每次都會問）');
    const on = state === 'session' || state === 'permanent';
    aabToggle.setAttribute('aria-checked', on ? 'true' : 'false');
    aabToggle.classList.toggle('on', on);
    aabPersist.checked = state === 'permanent';
  }

  async function loadAab() {
    try { const r = await fetch('/api/auto-approve-build'); renderAab(await r.json()); } catch {}
  }
  async function setAab(state) {
    try {
      const r = await fetch('/api/auto-approve-build', {
        method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ state }),
      });
      const d = await r.json();
      if (!r.ok) { pushToast({ title: '設定失敗', msg: d.error || '' }); return; }
      const hook = await (await fetch('/api/auto-approve-build')).json();
      renderAab(hook);
      if (state === 'permanent') pushToast({ title: '自動核准：永久', msg: 'build/test/install 跨重啟自動核准（可隨時關）' });
      else if (state === 'session') pushToast({ title: '自動核准：本次', msg: '關 dashboard 後自動失效' });
    } catch (err) { pushToast({ title: '設定失敗', msg: err.message }); }
  }

  aabToggle.addEventListener('click', () => {
    if (aabToggle.disabled) return;
    const on = aabToggle.classList.contains('on');
    setAab(on ? 'off' : (aabPersist.checked ? 'permanent' : 'session'));
  });
  aabPersist.addEventListener('change', () => {
    if (aabPersist.disabled) return;
    const on = aabToggle.classList.contains('on');
    if (!on) return;                          // checkbox 只在已開時切換永久/本次
    setAab(aabPersist.checked ? 'permanent' : 'session');
  });
  loadAab();
```

- [ ] **Step 3: CSS（`.switch.on` 若 styles.css 尚無 on 視覺，補一條；沿用既有 `--accent`）**

```css
  .switch.on { background: var(--accent); }
  .switch.on .switch-knob { transform: translateX(16px); }
```
（若既有 `.switch[aria-checked="true"]` 已有樣式，改用該 selector、刪本步驟。實作時先檢查 styles.css 既有 `.switch`。）

- [ ] **Step 4: 人工測試清單**

前提：已跑 `install-hooks.ps1`。Run: `cd server && node index.js` → 開 dashboard → 齒輪設定。
- [ ] 未裝 hook（暫移走 `~/.claude/hooks/auto-approve-build.sh`）→ toggle 變灰 + 提示「需先跑 install-hooks.ps1」。
- [ ] 裝了 hook → toggle 可用、預設關。
- [ ] 開 toggle（本次）→ 提示「本次」；到某真實 session 跑 `mvn -q test`（或 `npm test`）**不跳權限**；跑 `mvn test > out.txt` **仍跳**（被硬 deny）。
- [ ] 勾「永久套用」→ 重啟 dashboard（restart 鈕）→ toggle 仍為開、永久。
- [ ] 關 toggle → `auto-approve-build.enabled` 與 `.persist` 皆清除；回「每次都問」。
- [ ] 檢查 `~/.claude/auto-approve.log` 有 `Bash(build)` audit 紀錄。

- [ ] **Step 5: Commit（建議）**

```bash
git add web/dashboard.html web/ui/app.js web/ui/styles.css
git commit -m "feat(F16): FE 自動核准控制（主開關 + 永久 checkbox + capability gate）"
```

---

## 收尾

- [ ] **集中全測**：`cd server && node --test`（fslist / auto-approve-build / hook 三組 + 既有 win-helpers/usage 測試全綠）。
- [ ] **更新 feature.md / CHANGELOG**：F15 / F16 / F21 各一條（走 `release` skill 發 v0.2.2）。
- [ ] **todo.md**：發版時把 F15 / F16 / F21 從 Roadmap + Details 移除（`release` skill 規範）。
- [ ] **SETUP.md / README**：F16 hook 加入「選配增強」段（指 `install-hooks.ps1`）；F15 隱私註記（Chrome 音訊送 Google）。

---

## Self-Review（plan vs spec）

**Spec coverage：**
- F15 範圍（只 send-prompt/zh-TW/降級/隱私）→ Task A1 ✅
- F16 獨立第二支 hook + 放行/硬 deny → Task C1 ✅；最小 installer → C2 ✅；三段式旗標 + 啟動對帳/關閉清 → C3 ✅；capability gate + 主開關 + 永久 → C4 ✅；install 納入（postinstall 風險）→ C1 whitelist + audit ✅
- F21 fs/list → B1+B2 ✅；launch-session（wt→powershell）→ B2 ✅；modal + 記憶上次 + 確認 → B3 ✅
- 多 hook 合成查證 → 已於 plan 前置完成（claude-code-guide）✅
- 測試（hook spawn / 狀態機 / fs.list node:test；其餘人工清單明列）✅

**Placeholder scan：** 無 TBD/TODO；每個 code step 附完整 code。CSS Step C4-3 註明「先檢查既有 `.switch`」屬合理條件分支，非 placeholder。

**Type/名稱一致：** `listDir`/`driveRoots`（fslist）、`readState/setState/reconcileOnStartup/clearOnShutdown/hookInstalled/enabledPath/persistPath`（auto-approve-build，測試與 index.js 用名一致）、`launchClaudeSession`、`renderAab/loadAab/setAab`、旗標檔名 `auto-approve-build.{enabled,persist}` 全文一致。
